{
  "name": "tui-portfolio",
  "version": "0.0.1",
  "description": "A TUI portfolio app built with Ink",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "tui-portfolio": "./dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/cli.jsx --bundle --platform=node --format=esm --outfile=dist/cli.js --external:ink --external:react",
    "start": "node dist/cli.js",
    "dev": "npm run build && node dist/cli.js"
  },
  "keywords": [
    "tui",
    "cli",
    "ink",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "goober": "^2.1.14",
    "ink": "^5.0.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
